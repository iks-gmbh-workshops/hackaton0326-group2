spring:
  application:
    name: reme-backend

  datasource:
    url: jdbc:postgresql://localhost:5432/reme
    username: ${DB_USERNAME:reme}
    password: ${DB_PASSWORD:reme}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration

  mail:
    host: ${MAIL_HOST:localhost}
    port: ${MAIL_PORT:1025}
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}

server:
  port: 8080

jwt:
  secret: ${JWT_SECRET:changeme-in-production}
  expiration-ms: 86400000
